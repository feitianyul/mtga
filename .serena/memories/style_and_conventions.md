# 编码与约定
- 目标 Python 3.13，统一四空格缩进，遵循 PEP 8（模块顶部集中导入，函数使用 `snake_case`、类用 `CamelCase`、常量全大写）。
- 平台相关逻辑需要参考 `setup_environment()` 的模式进行保护，避免 Finder/打包环境的工作目录或 Tk 库路径回归。
- 证书/hosts 操作属于安全敏感域，必须避免硬编码私钥或 entitlements 变更，必要时在描述中说明风险及缓解措施。
- 文案/日志预设中文输出，GUI/日志信息要简洁清晰，并在复杂流程前增加提示。