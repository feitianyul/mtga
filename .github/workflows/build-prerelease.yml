name: 🚀 构建和发布版本

on:
  push:
    tags:
      - 'v*.*.*-*'  # 预发布版标签格式：v1.2.0-beta.1, v1.2.0-rc.1
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: 1.2.0-beta.1)'
        required: true
        default: '1.2.0-beta.1'
      release_type:
        description: '发布类型'
        required: true
        default: 'prerelease'
        type: choice
        options:
          - prerelease
          - release
      start_commit:
        description: >-
          起始提交哈希（示例：
          1234567890abcdef1234567890abcdef12345678）
        required: true

env:
  PYTHON_VERSION: '3.13'

jobs:
  build-macos:
    name: 🍎 构建 macOS
    runs-on: macos-latest
    strategy:
      matrix:
        arch: 
          # - x64
          - arm64
        include:
          # - arch: x64
          #   runner_arch: x86_64
          #   nuitka_arch: x86_64
          - arch: arm64  
            runner_arch: arm64
            nuitka_arch: arm64

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 安装 uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: 💾 缓存 uv 依赖
        uses: actions/cache@v4
        with:
          path: |
            .venv
            ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ matrix.arch }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-${{ matrix.arch }}-

      - name: 🔧 安装依赖
        run: |
          uv sync --extra mac-build

      - name: 💾 缓存 Nuitka 工具
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/share/Nuitka
            ~/.cache/nuitka
          key: ${{ runner.os }}-nuitka-${{ matrix.arch }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-nuitka-${{ matrix.arch }}-
      - name: 🛠️ 安装 Xcode Command Line Tools
        run: |
          sudo xcode-select --install || true
          # 等待安装完成或跳过如果已安装
          while ! xcode-select -p &>/dev/null; do
            echo "等待 Xcode Command Line Tools 安装..."
            sleep 10
          done

      - name: ⚙️ 获取版本信息
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/v}
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "APP_NAME=mtga_gui" >> $GITHUB_OUTPUT
          echo "DMG_NAME=MTGA_GUI-v$VERSION-${{ matrix.arch }}" >> $GITHUB_OUTPUT

      - name: 🔨 构建 macOS 应用包
        run: |
          # 修改构建脚本中的版本号
          sed -i.bak "s/VERSION=\".*\"/VERSION=\"${{ steps.version.outputs.VERSION }}\"/g" build_mac_app.sh
          
          # 根据架构设置输出文件名，替换 $(uname -m) 为具体架构
          sed -i.bak "s/\$(uname -m)/${{ matrix.arch }}/g" build_mac_app.sh
          
          # 显示修改后的构建脚本相关行，用于调试
          echo "🔍 检查修改后的构建脚本:"
          echo "VERSION 行:"
          grep "VERSION=" build_mac_app.sh | head -1
          echo "output-filename 行:"
          grep "output-filename" build_mac_app.sh
          
          # 执行构建
          chmod +x build_mac_app.sh
          ./build_mac_app.sh
          
          # Nuitka 在 macOS 应用包模式下忽略 --output-filename，总是使用源文件名
          # 需要手动重命名到期望的文件名
          # if [ -d "dist-onefile/mtga_gui.app" ]; then
          #   EXPECTED_NAME="${{ steps.version.outputs.APP_NAME }}.app"
          #   echo "🔄 重命名应用包: mtga_gui.app → $EXPECTED_NAME"
          #   mv "dist-onefile/mtga_gui.app" "dist-onefile/$EXPECTED_NAME"
          #   echo "✅ 重命名完成"
          # fi

      - name: 📱 验证应用包
        run: |
          echo "🔍 检查 dist-onefile 目录内容:"
          ls -la dist-onefile/ || echo "❌ dist-onefile 目录不存在"
          
          # 查找实际生成的 .app 文件
          APP_FILES=(dist-onefile/*.app)
          if [ ! -e "${APP_FILES[0]}" ]; then
            echo "❌ 没有找到 .app 文件"
            echo "📁 当前目录结构:"
            find dist-onefile/ -type f -name "*.app" 2>/dev/null || echo "没有找到任何 .app 文件"
            exit 1
          fi
          
          # 使用找到的第一个 .app 文件
          ACTUAL_APP_PATH="${APP_FILES[0]}"
          echo "✅ 应用包构建成功: $ACTUAL_APP_PATH"
          echo "📊 应用包大小: $(du -sh "$ACTUAL_APP_PATH" | cut -f1)"
          echo "📁 应用包结构:"
          find "$ACTUAL_APP_PATH" -maxdepth 2 -type d | head -10
          
          # 检查期望的文件名是否匹配
          EXPECTED_PATH="dist-onefile/${{ steps.version.outputs.APP_NAME }}.app"
          if [ "$ACTUAL_APP_PATH" != "$EXPECTED_PATH" ]; then
            echo "⚠️ 警告: 实际文件名与期望不符"
            echo "期望: $EXPECTED_PATH"
            echo "实际: $ACTUAL_APP_PATH"
          fi

      - name: 🎁 创建 DMG 安装包
        run: |
          # 确保 DMG 配置文件存在
          if [ ! -f "mac/dmg_settings.py" ]; then
            echo "❌ DMG 配置文件不存在: mac/dmg_settings.py"
            exit 1
          fi
          
          # 查找实际的 .app 文件
          APP_FILES=(dist-onefile/*.app)
          if [ ! -e "${APP_FILES[0]}" ]; then
            echo "❌ 没有找到 .app 文件用于创建 DMG"
            exit 1
          fi
          
          ACTUAL_APP_PATH="${APP_FILES[0]}"
          
          echo "📦 使用应用包: $ACTUAL_APP_PATH"
          echo "📦 DMG 文件名: ${{ steps.version.outputs.DMG_NAME }}.dmg"
          
          # 设置环境变量供DMG配置文件使用，包含完整路径
          export MTGA_APP_NAME="$ACTUAL_APP_PATH"
          echo "🔧 设置 DMG 应用路径: $MTGA_APP_NAME"
          
          # 创建 DMG
          uv run dmgbuild \
            -s mac/dmg_settings.py \
            "MTGA GUI v${{ steps.version.outputs.VERSION }} Installer" \
            "dist-onefile/${{ steps.version.outputs.DMG_NAME }}.dmg"
          
          echo "✅ DMG 创建成功"
          echo "📊 DMG 大小: $(du -sh "dist-onefile/${{ steps.version.outputs.DMG_NAME }}.dmg" | cut -f1)"

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.arch }}-artifacts
          path: |
            dist-onefile/*.app
            dist-onefile/*.dmg
          retention-days: 7

  # Windows 构建任务（预留框架）
  build-windows:
    name: 🪟 构建 Windows
    runs-on: windows-latest

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 安装 uv
        run: |
          Invoke-WebRequest -Uri "https://astral.sh/uv/install.ps1" -OutFile "install.ps1"
          ./install.ps1
          echo "$env:USERPROFILE\.local\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - name: 💾 缓存 uv 依赖
        uses: actions/cache@v4
        with:
          path: |
            .venv
            ${{ env.USERPROFILE }}\.cache\uv
          key: ${{ runner.os }}-uv-win-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-win-

      - name: 🔧 安装依赖
        run: |
          uv sync --extra win-build

      - name: 💾 缓存 Nuitka 工具
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.USERPROFILE }}\AppData\Local\Nuitka
          key: ${{ runner.os }}-nuitka-win-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-nuitka-win-

      - name: 🔍 检查 Visual Studio
        run: |
          $vcBuildDir = $null

          $vswherePath = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
          if (Test-Path $vswherePath) {
            try {
              $installationPath = & $vswherePath -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
              if ($installationPath) {
                $candidate = Join-Path $installationPath "VC\Auxiliary\Build"
                if (Test-Path $candidate) {
                  $vcBuildDir = $candidate
                }
              }
            } catch {
              Write-Host "⚠️ vswhere 检测失败: $_"
            }
          }

          if (-not $vcBuildDir) {
            $fallbackPaths = @(
              "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build",
              "C:\Program Files\Microsoft Visual Studio\2022\Professional\VC\Auxiliary\Build",
              "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build",
              "C:\Program Files\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build",
              "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Auxiliary\Build",
              "C:\Program Files (x86)\Microsoft Visual Studio\2019\Professional\VC\Auxiliary\Build",
              "C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\VC\Auxiliary\Build",
              "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Auxiliary\Build"
            )

            foreach ($path in $fallbackPaths) {
              if (Test-Path $path) {
                $vcBuildDir = $path
                break
              }
            }
          }

          if ($vcBuildDir) {
            Write-Host "✅ 找到可用的 MSVC 环境: $vcBuildDir"
            "VC_BUILD_DIR=$vcBuildDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          } else {
            Write-Host "❌ 未找到任何已安装的 Visual Studio MSVC 环境，后续构建可能失败"
          }

      - name: ⚙️ 获取版本信息
        id: version
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $VERSION = "${{ github.event.inputs.version }}"
          } else {
            $VERSION = "${{ github.ref }}" -replace "refs/tags/v", ""
          }
          echo "VERSION=$VERSION" >> $env:GITHUB_OUTPUT
          echo "EXE_NAME=MTGA_GUI-v$VERSION-x64.exe" >> $env:GITHUB_OUTPUT

      - name: 🔨 构建 Windows 单文件版本
        run: |
          # 修改构建脚本中的版本号
          $content = Get-Content "build_onefile.bat"
          $content = $content -replace 'set "VERSION=.*"', "set `"VERSION=${{ steps.version.outputs.VERSION }}`""
          $content | Set-Content "build_onefile.bat"

          # 显示修改后的版本号
          Write-Host "🔍 检查修改后的构建脚本:"
          Select-String -Path "build_onefile.bat" -Pattern "VERSION=" | Select-Object -First 1

          # 执行构建
          try {
            if ($env:VC_BUILD_DIR) {
              Write-Host "ℹ️ 传递检测到的 MSVC 目录: $env:VC_BUILD_DIR"
              & .\build_onefile.bat "$env:VC_BUILD_DIR"
            } else {
              & .\build_onefile.bat
            }
            Write-Host "✅ 构建脚本执行完成"
          } catch {
            Write-Host "❌ 构建失败: $_"
            exit 1
          }

      - name: 📱 验证可执行文件
        run: |
          $expectedName = "${{ steps.version.outputs.EXE_NAME }}"
          $expectedPath = Join-Path "dist-onefile" $expectedName

          if (-not (Test-Path "dist-onefile")) {
            Write-Host "❌ dist-onefile 目录不存在，无法找到构建产物"
            exit 1
          }

          if (-not (Test-Path $expectedPath)) {
            $latestExe = Get-ChildItem -Path "dist-onefile" -Filter *.exe -File -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1
            if ($latestExe) {
              Write-Host "⚠️ 发现未命名为期望格式的可执行文件: $($latestExe.Name)"
              Rename-Item -Path $latestExe.FullName -NewName $expectedName -Force
            }
          }

          if (Test-Path $expectedPath) {
            $size = (Get-Item $expectedPath).Length / 1MB
            Write-Host "✅ Windows 可执行文件构建成功: $expectedPath"
            Write-Host "📊 文件大小: $([math]::Round($size, 2)) MB"

            $fileInfo = Get-Item $expectedPath
            Write-Host "📋 文件信息:"
            Write-Host "  - 创建时间: $($fileInfo.CreationTime)"
            Write-Host "  - 修改时间: $($fileInfo.LastWriteTime)"
            Write-Host "  - 文件属性: $($fileInfo.Attributes)"
            "WINDOWS_EXE_PATH=$expectedPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          } else {
            Write-Host "❌ 未找到预期的可执行文件: $expectedPath"
            Write-Host "📁 dist-onefile 目录内容:"
            Get-ChildItem -Path "dist-onefile" -Recurse | ForEach-Object {
              if ($_.PSIsContainer) {
                Write-Host "  [目录] $($_.FullName)"
              } else {
                Write-Host "  $($_.FullName) ($([math]::Round($_.Length/1MB, 2)) MB)"
              }
            }
            exit 1
          }

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: |
            dist-onefile/*.exe
          retention-days: 7

  create-release:
    name: 🚀 创建 GitHub 发布
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: ⚙️ 获取版本信息
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
            TAG_NAME="v$VERSION"
            RELEASE_TYPE="${{ github.event.inputs.release_type }}"
          else
            VERSION=${GITHUB_REF#refs/tags/v}
            TAG_NAME=${GITHUB_REF#refs/tags/}
            if [[ "${TAG_NAME}" == *"-"* ]]; then
              RELEASE_TYPE="prerelease"
            else
              RELEASE_TYPE="release"
            fi
          fi

          if [ "$RELEASE_TYPE" = "release" ]; then
            RELEASE_LABEL="正式发布版"
            RELEASE_NAME="🎉 MTGA GUI v${VERSION}"
          else
            RELEASE_LABEL="预发布版"
            RELEASE_NAME="🚀 MTGA GUI v${VERSION}"
          fi

          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "RELEASE_TYPE=$RELEASE_TYPE" >> $GITHUB_OUTPUT
          echo "RELEASE_LABEL=$RELEASE_LABEL" >> $GITHUB_OUTPUT
          echo "RELEASE_NAME=$RELEASE_NAME" >> $GITHUB_OUTPUT

      - name: 📥 下载所有构建产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 📋 列出构建产物
        run: |
          echo "🗂️ 构建产物列表:"
          find artifacts -type f -name "*.app" -o -name "*.dmg" -o -name "*.exe" | sort

      - name: 🏷️ 创建或更新标签
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          LABEL="${{ steps.version.outputs.RELEASE_LABEL }}"
          if [ "$LABEL" = "预发布版" ]; then
            EMOJI="🚀"
          else
            EMOJI="🎉"
          fi
          
          # 删除已存在的标签（如果有）
          git tag -d ${{ steps.version.outputs.TAG_NAME }} || true
          git push origin :refs/tags/${{ steps.version.outputs.TAG_NAME }} || true
          
          # 创建新标签
          git tag -a ${{ steps.version.outputs.TAG_NAME }} -m "$EMOJI $LABEL ${{ steps.version.outputs.VERSION }}"
          git push origin ${{ steps.version.outputs.TAG_NAME }}

      - name: 📝 生成发布说明
        id: release_notes
        run: |
          TAG_NAME="${{ steps.version.outputs.TAG_NAME }}"
          START_COMMIT_INPUT="${{ github.event.inputs.start_commit || '' }}"
          TARGET_COMMIT="${{ github.sha }}"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ -z "$START_COMMIT_INPUT" ]; then
              echo "❌ 手动触发工作流时必须填写起始提交哈希"
              exit 1
            fi
            if ! git cat-file -e "$START_COMMIT_INPUT^{commit}" 2>/dev/null; then
              echo "❌ 无法在仓库中找到起始提交：$START_COMMIT_INPUT"
              exit 1
            fi

            START_COMMIT=$(git rev-parse "$START_COMMIT_INPUT")

            if ! git merge-base --is-ancestor "$START_COMMIT" "$TARGET_COMMIT"; then
              echo "❌ 起始提交不在当前构建提交的历史中：$START_COMMIT_INPUT"
              exit 1
            fi

            : > release_notes.md

            START_SUBJECT=$(git show -s --format='%s' "$START_COMMIT")
            if echo "$START_SUBJECT" | grep -Eq '^(feat|fix)\b'; then
              echo "- $START_SUBJECT ($START_COMMIT)" >> release_notes.md
            fi

            git log --format='%H%x09%s' --reverse "$START_COMMIT..$TARGET_COMMIT" | while IFS=$'\t' read -r HASH SUBJECT; do
              if echo "$SUBJECT" | grep -Eq '^(feat|fix)\b'; then
                echo "- $SUBJECT ($HASH)" >> release_notes.md
              fi
            done

            MATCH_TOTAL=$(grep -c '^- ' release_notes.md || true)
            if [ "$MATCH_TOTAL" -eq 0 ]; then
              echo "- 暂无匹配的 feat/fix 提交" >> release_notes.md
            fi
          else
            git show --no-patch --format='- %s' "${{ github.sha }}" > release_notes.md 2>/dev/null || true
            if [ ! -s release_notes.md ]; then
              printf '%s\n' "$TAG_NAME" > release_notes.md
            fi
          fi

          echo "RELEASE_NOTES<<EOF" >> $GITHUB_OUTPUT
          cat release_notes.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: 🚀 创建 GitHub 发布
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.version.outputs.TAG_NAME }}
          name: ${{ steps.version.outputs.RELEASE_NAME }}
          body: ${{ steps.release_notes.outputs.RELEASE_NOTES }}
          prerelease: ${{ steps.version.outputs.RELEASE_TYPE != 'release' }}
          draft: false
          artifacts: |
            artifacts/windows-artifacts/*.exe
            artifacts/macos-x64-artifacts/*.dmg
            artifacts/macos-arm64-artifacts/*.dmg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ 发布完成
        run: |
          echo "🎉 ${{ steps.version.outputs.RELEASE_LABEL }} v${{ steps.version.outputs.VERSION }} 发布完成!"
          echo "🔗 发布页面: https://github.com/${{ github.repository }}/releases/tag/${{ steps.version.outputs.TAG_NAME }}"
